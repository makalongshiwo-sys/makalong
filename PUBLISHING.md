# 从“分析并更新”到原生 App

用户在当前维护项目中提出 BTC/ETH/SOL 分析，并明确说“更新”后，助手执行下列完整流程；用户不需要自己导入 JSON。

1. 获取并核验本次证据，按三币独立整理趋势、机制健康、事件风险、缺口、反证和复核期限。公开报告排除个人账户、成本、仓位、挂单、聊天原文和所有凭证。
2. 读取仓库 main 最新 content/reports.json。只追加新一期唯一 ID，不覆盖已发布判断。事件与解释卡使用该期 events / qa 引用，避免串入其他期内容。
3. 使用 scripts/prepare-report.py 验证新报告。助手通过已授权的 GitHub 连接提交报告；更新只涉及内容时，无需重新编译 APK。
4. 提交后从公开地址读回完全相同的 revision 与新报告 ID。只有写入和读回都成功，才答复“已更新”。工具权限或网络失败时明确说明失败，不能把草稿当发布成功。
5. App 的研报页每分钟前台检查、重新进入时检查；失败显示缓存与失败原因。启用后台提醒后，Android 定期检查新市场报告并去重。读到旧版不是发布了新版。

公开读取地址：
https://raw.githubusercontent.com/makalongshiwo-sys/makalong/main/content/reports.json

该机制不监听所有 ChatGPT 会话；需要由有本仓库连接的助手实际执行上述步骤。不创建额外每日研究任务。不在 APK 内放 GitHub 写入令牌。

市场价格与研究是两条更新链：价格由 App 直接读取 Binance，前台约15秒更新；研究由本次对话核验后发布。ETF 是另外的有日期披露快照，现有数据不能声称全天候自动供应商同步。

即时后台云推送仍需配置 FCM 或手机厂商推送服务、后端行情采集与设备订阅。当前 JobScheduler 实现受 Android 调度限制，不承诺即时到达。
